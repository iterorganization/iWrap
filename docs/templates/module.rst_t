{%- if show_headings %}
{{- basename[basename.rfind('.')+1:] | e | heading(2) }}
{% endif -%}

.. automodule:: {{ qualname }}
   :noindex:
{%- for option in automodule_options %}
   :{{ option }}:
{%- endfor %}