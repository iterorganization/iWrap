{% macro imports() -%}
    import imas
{%- endmacro -%}

{% macro declare(ids_name, ids_var_name) -%}
     {{ ids_var_name }} = imas.{{ ids_name }}()
{%- endmacro -%}

{%- macro provenance(ids_var_name, sbrt_name) -%}
   {{ ids_var_name }}.code.name = "{{sbrt_name}}"
   {{ ids_var_name }}.code.version = ""
{%- endmacro -%}
